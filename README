Name: Daryl Ng

Install:
1. In Ubuntu, run 'sudo apt-get install gcc-multilib' to install 32-bit platform.
Note: Remaking the object files cause a segmentation fault in Ubuntu.

Run LWP:
1. Run command "./numbersmain".